import { ApiExtensionContext } from "@directus/shared/types";
import { RegisterFunctions } from "../../@types/directus";


export default function ({ init, action }: RegisterFunctions, { database }: ApiExtensionContext) {
    // action('server.start', async (meta: Record<string, any>, { schema, accountability }) => {

    // })
};




