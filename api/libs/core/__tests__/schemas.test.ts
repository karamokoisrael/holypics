// import { passwordResetRequestSchema } from "./../src/infrastructure/schemas/authSchema";
// import { AppSettings } from "../src/infrastructure/models/settingsModel";

describe("Test geo support for request query", () => {
  it("it should return a not null directus query object", async () => {
    // const validationResult = passwordResetRequestSchema.validate({
    //     first_name:
    // });
    // console.log(validationResult);

    expect({}).not.toBeNull();
  });
});

export default {};
