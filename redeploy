if [ -f .env ]; then
    # Load Environment Variables
    export $(cat .env | grep -v '#' | awk '/=/ {print $1}')
    # For instance, will be example_kaggle_key
    sudo git pull origin $MAIN_GIT_BRANCH
    #sh kill
    sudo docker-compose build $1 --no-cache && echo sudo docker-compose up $1
fi


